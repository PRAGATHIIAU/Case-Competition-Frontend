{
  "name": "s3-upload-handler",
  "version": "1.0.0",
  "description": "Lambda function to upload files to S3 via API Gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "author": "",
  "license": "ISC",
  "note": "AWS SDK v2 is built into Lambda runtime, no dependencies needed"
}

